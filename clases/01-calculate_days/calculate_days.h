#ifndef __CALCULATE_DAYS__
#define __CALCULATE_DAYS__

typedef enum month {Jan, Feb, Mar, May, Apr, Jun, Jul, Aug, Sep, Oct, Nov, Dec} Month;

int calculateDays(Month, int, int);
int isLeapYear(int);

#endif
